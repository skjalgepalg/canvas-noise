export const params = {
  // Seed
  seed: 'snerk-2025-08-24',
  // Noise
  noiseType: '2D',
  noiseOffset: 0,
  noiseScale: 0.007,
  noiseRadius: 1.6,
  noiseSpeed: 1,
  // Animation
  compositeOperation: 'lighter',
  // Recording
  record: false, // Set true and reload to save a recording
  frameRate: 25,
  duration: 5,
  // Canvas
  fillScreen: true,
  xDim: 1080,
  yDim: 1920,
  margin: 55,
  bgColor: '#000',
}
